<% @cookie_value = cookies[:rackjamnotice] %>

<% if @cookie_value != 'closed' %>
	<div id="intro">
		<%= Twitter.user_timeline("rackjam").first.text %>
		<a href="#" id="closeIntro">x</a>
	</div>
<% end %>
